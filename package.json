{
  "name": "cloudflare-speedtest-node",
  "version": "0.2.0",
  "private": true,
  "description": "Per-node Cloudflare speedtest + geocheck agent that pairs with the aerio panel",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  }
}
