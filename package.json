{
  "name": "cloudflare-speedtest-node",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare speedtest agent — HTTP endpoint that runs speedtest on demand",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  }
}
